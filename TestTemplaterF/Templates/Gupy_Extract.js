// extractData.js

// Función de extracción de datos
const extractData = function () {
    //[2023-07-13;TPL_Gupy_V02] //Do not Delete
    const out = {};
    const jobs = [];

    jobs.push({ title: "DIamantes" })

    out.jobs = jobs;
    return out;
};

// Exportar la función
module.exports = extractData;
